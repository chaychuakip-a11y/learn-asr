"""Speaker-disjoint FSDD generalization experiment.

Import concrete APIs from :mod:`fsdd_generalization.data` or
:mod:`fsdd_generalization.training`. Keeping package import side-effect free also
allows ``python -m fsdd_generalization.data`` to run without preloading itself.
"""
