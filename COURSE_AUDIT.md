# 课程审计与教学边界

## 本轮升级

41 本源 Notebook 已统一增加：

- 前置要求、建议时间、核心概念和完成标准；
- 3 题闭卷课前诊断；
- 12 道逐课专属题，覆盖回忆、预测、计算、编程、排错、迁移和表达；
- 24 分自评量表与进入下一课的门槛；
- 第 1、7、30 天间隔复习计划；
- 课程阶段、课号和升级版本 metadata。

## 审计原则

1. 代码能够运行不等于教学结论成立，关键实验必须检查数值和边界。
2. 每个模块必须说明输入输出、shape、单位、状态、延迟和失败模式。
3. 教学基线必须明确其局限，不能用训练集过拟合冒充泛化。
4. 性能只能在目标硬件、固定线程和可复现输入上比较。
5. 语义后处理必须保留原始证据，不能用“更合理”的文本覆盖不确定语音。

## 已知教学限制

| 课程 | 限制 | 正确使用方式 |
|---:|---|---|
| 14 | 22 条小样本过拟合，双向 GRU 非流式 | 用于观察 CTC loss 与 spike；不要引用为测试准确率 |
| 17 | PGS 事件为教学模拟 | 接入具体厂商时以其字段边界和重连协议为准 |
| 19 | Add-k bigram 很小 | 用于理解概率；工业 LM 需 Kneser-Ney/backoff 与领域数据 |
| 20 | LM 融合实现为了可读性简化 | 生产实现应使用 log-space、批量剪枝和完整测试 |
| 21～23 | 纯 Python 小图 | 用于理解 WFST；大图应使用 OpenFst/Kaldi/k2 类实现 |
| 24 | 声学后验是可控模拟 | 用于验收状态和协议，不用于报告 ASR 质量 |
| 25～30 | 部署模型为随机确定性权重 | 用于 ONNX/cache/INT8/API；替换为训练模型后重新验收 |
| 32 | Spectral gate 与 AGC 是教学基线 | 必须用真实噪声和 CER/WER 验证 |
| 34 | NLMS 未完整实现生产 double-talk/漂移 | 用于理解 reference、自适应路径和 ERLE |
| 35 | 两麦克风整数延迟模拟 | 真实阵列需几何校准、分数延迟和混响处理 |
| 37 | 少量 speaker prototype 且非严格独立评估 | 用于理解 embedding 接口，不报告生产 DER |
| 38 | ITN 规则只覆盖少量例子 | 号码、数量、日期、金额应分类建模并保留 spoken form |
| 40 | 小型字符袋分类器 | 用于 NLU contract；不代表真实领域泛化 |
| 41 | 不调用外部 LLM | 重点是证据、schema、验证、确认和审计设计 |

## 后续真实项目的最低要求

- speaker-disjoint 的训练、验证和测试集；
- 固定文本规范和 CER/WER 脚本；
- 流式与离线分别评估；
- 前端噪声、设备、语速和长度分桶；
- FP32/INT8、不同 beam/LM/chunk 的质量—延迟曲线；
- 版本化模型、词表、LM、WFST graph、前端参数和语义 schema。
