# 课程审计与教学边界

## 本轮升级

### 唯一能力路线

根目录新增 `LEARNING_PATH.md`，将全部材料按依赖重排为阶段 0～12。CTC、流式、PGS、RTF、LM/WFST、量化部署、麦克风前端、语义后处理和真实泛化评测均属于必修主干；语言模型专修 01～08 与主线 19～24 改为交错学习，消除原路线的重复和先后冲突。`notebooks/README.md` 只负责定位材料，不再维护第二套独立顺序。

每个阶段现在明确给出建议时间、必做实现、失败实验、产出和离场标准。最终掌握仍由知识、编码、结课 Notebook、迁移故障注入和口述答辩五类证据共同证明。

### 完课清单

课程的材料边界现已固定为：1 本含 14 个可运行小节的零基础逐行代码伴读、6 节 PyTorch 桥梁课、6 节音频零基础桥梁课、6 节研究与工程思维课、8 节认知拓展课、46 节 ASR 主线、9 节语言模型路线、学习中枢、9 个专题实验室和 1 个结课项目。所有课程、专题和项目同时提供无输出学习版与已运行对照；`scripts/execute_course_labs.py` 默认以无界面独立进程逐 cell 复验，加入 `--write-executed` 时使用 Jupyter 内核重新生成对照。

专题覆盖 CTC、流式、WFST、量化部署、音频前端、语义后处理、FSDD speaker-disjoint、FSDD 六折 LOSO 和 AudioMNIST 外部盲测。统一索引不再引用某台电脑的临时可视化路径。

零基础入口由单节导学扩展为 6 节 PyTorch 桥梁课，覆盖 Python 最小语法、Tensor/shape、广播与维度变换、autograd、`nn.Module`、训练验证循环、`Dataset`/`DataLoader`、变长语音 padding/length/mask。每节同时提供无输出学习版和真实内核执行过的对照版，并保留课前诊断、10 道练习、离场票与间隔复习。

Python/PyTorch 与 ASR 主线之间新增 6 节音频零基础桥梁课，按振动与波形、周期/频率/相位、peak/RMS/功率/dB、采样/量化/PCM/位深/通道、叠加/谐波/噪声/SNR、真实 WAV 审计递进。每节固定使用“物理含义、单位、数组表示、参数变化”解释框架，包含 24 分分层练习、从空白实现、故障输入和第 1/7/30 天复习；所有学习版均有真实内核执行过的对照版。

音频桥梁之后新增专业 GUI 软件实验：Audacity 负责波形、声谱图、FFT、DC、削波与稳态降噪；Praat 负责 F0、Formant、Intensity 与 TextGrid；Sonic Visualiser 和 Adobe Audition/Cool Edit 风格工作流作为选修。仓库提供 7 条确定性校准/故障音频与 `manifest.json`，要求 GUI、manifest 和 Notebook 三方核对，并明确 dBFS、未校准 intensity、显示参数与真实样本修改之间的边界。

GUI 基础之后新增 24 条盲命名诊断题，覆盖低增益、双向/单向削波、DC、50 Hz 嗡声、宽带噪声、窄带啸叫、click/dropout、回声/混响、低通/高通、低位深、增益抽吸、错误采样率、单边无声、反相、通道延迟、边界截断、无抗混叠重采样和重度非线性压缩，并混入正常单/双通道对照。答案通过 CLI 按 hint/check/reveal 渐进释放，防止文件名泄题或强行给正常样本找故障。

新增 6 节研究与工程思维课，把 NIST 实验设计与 SCTK 评测、Bootstrap/校准、Model Cards、Datasheets 和 AI 风险管理转成可运行的 ASR 小实验。课程固定训练“主张—证据—反证—边界—下一步”证据卡，覆盖可证伪假设、S/D/I 与切片、辛普森悖论、因子实验与交互、按说话人配对 Bootstrap、不变量/变形/反事实测试，以及硬约束、Pareto 前沿、风险和决策记录。课程同时提供学习拓展调研，明确语音学、数据中心 ASR、多语口音、人机交互、隐私安全、端侧系统与事故响应等后续方向。

新增 8 节跨领域认知拓展课与总手册，把逻辑反例、自然频数与贝叶斯、因果图、存量/反馈/延迟、期望值/后悔/信息价值、Shannon 熵与互信息、Brier 评分与预测校准、提取/间隔/反馈/迁移做成可运行小实验。每课强制包含一手资料、误用警报、适用边界和 ASR/工作/生活三场景迁移，避免退化为只会背诵的“思维模型清单”。

在桥梁课之前增加逐行代码伴读：每个小节固定包含“运行前怎样读、带中文注释的代码、运行后核对、常见错误、只改一处的小实验”，覆盖 Python 数据流、NumPy/PyTorch shape、dtype/device、`nn.Module`、训练一步、变长 batch、声学前端、CTC loss/解码、流式 cache 和系统化排错。验证器要求至少 14 个代码单元及完整执行对照，防止代码伴读退化为只有概念没有运行证据的文档。

ASR 主线由第 1～41 课扩展到第 46 课：第 42 课连接 Conv1d、注意力与 Conformer 的流式边界；第 43 课用可穷举小例子验证 RNN-T 二维对齐，并解释 TDT duration 跳帧；第 44 课讲自监督遮挡预训练；第 45 课搭建教学规模的 AudioEncoder、projector 与小语言模型接口；第 46 课给出真实 Qwen3-ASR 项目的资源预检、推理基线、数据泄漏检查、微调顺序和验收表。

语言模型专修路线扩展为 9 课：从 Bigram、平滑与困惑度进入 OpenFst/KenLM、L/G/HCLG/CTC-TLG、N-best/lattice、闭卷验收与前沿 SpeechLLM/上下文系统设计。第 3～8 课依赖 WSL 工具链，并提供单独的安装指南和速查表。

新增实时数字 CTC 结课项目、`learning_workspace/` 亲手编码区、知识/编码/Notebook/迁移/口述五类掌握度门禁。课程材料与参考实现完成不等于学习者通过门禁。

课程根入口新增 `学习中枢_诊断与掌握度仪表盘.ipynb`：

- 24 道题覆盖声音特征、张量编码、CTC、流式、LM/WFST、部署、前端和语义八个阶段；
- 自动计算阶段分数、绘制雷达图并推荐最早的知识断点；
- 用采样率、帧长和帧移滑块联动展示波形分帧与单帧频谱；
- 用 0～4 级证据量表区分看懂、解释、实现、排错和迁移；
- 提供八项阶段综合任务和本地个人进度保存。

学习中枢已纳入仓库校验，并额外进行完整内核执行测试。

CTC 主线新增 `专题_CTC可视化实验室_从路径到流式解码.ipynb`，用于在同一可穷举样例上交叉验证路径概率、log-space 前向算法、PyTorch `CTCLoss`、梯度、Prefix Beam 与跨 chunk 状态一致性，避免把第 10～14 课学成互不关联的 API。

第 1～41 课的源 Notebook 已统一增加：

- 前置要求、建议时间、核心概念和完成标准；
- 3 题闭卷课前诊断；
- 12 道逐课专属题，覆盖回忆、预测、计算、编程、排错、迁移和表达；
- 24 分自评量表与进入下一课的门槛；
- 第 1、7、30 天间隔复习计划；
- 课程阶段、课号和升级版本 metadata。

第 1～46 课进一步统一增加“知识接力”卡：新课前先闭卷取回前一阶段的概念、单位、shape 和状态；只能答出零到一项时必须返回明确的补课入口。每张卡同时声明本课输入/输出契约，并要求识别一种“代码能运行但语义错误”的缺失信息。验证器要求每节学习版和运行对照恰好保留一张接力卡。

## 审计原则

1. 代码能够运行不等于教学结论成立，关键实验必须检查数值和边界。
2. 每个模块必须说明输入输出、shape、单位、状态、延迟和失败模式。
3. 教学基线必须明确其局限，不能用训练集过拟合冒充泛化。
4. 性能只能在目标硬件、固定线程和可复现输入上比较。
5. 语义后处理必须保留原始证据，不能用“更合理”的文本覆盖不确定语音。

## 已知教学限制

| 课程 | 限制 | 正确使用方式 |
|---:|---|---|
| 14 | 22 条小样本过拟合，双向 GRU 非流式 | 用于观察 CTC loss 与 spike；不要引用为测试准确率 |
| 17 | PGS 事件为教学模拟 | 接入具体厂商时以其字段边界和重连协议为准 |
| 19 | Add-k bigram 很小 | 用于理解概率；工业 LM 需 Kneser-Ney/backoff 与领域数据 |
| 20 | LM 融合实现为了可读性简化 | 生产实现应使用 log-space、批量剪枝和完整测试 |
| 21～23 | 纯 Python 小图 | 用于理解 WFST；大图应使用 OpenFst/Kaldi/k2 类实现 |
| 24 | 声学后验是可控模拟 | 用于验收状态和协议，不用于报告 ASR 质量 |
| 25～30 | 部署模型为随机确定性权重 | 用于 ONNX/cache/INT8/API；替换为训练模型后重新验收 |
| 32 | Spectral gate 与 AGC 是教学基线 | 必须用真实噪声和 CER/WER 验证 |
| 34 | NLMS 未完整实现生产 double-talk/漂移 | 用于理解 reference、自适应路径和 ERLE |
| 35 | 两麦克风整数延迟模拟 | 真实阵列需几何校准、分数延迟和混响处理 |
| 37 | 少量 speaker prototype 且非严格独立评估 | 用于理解 embedding 接口，不报告生产 DER |
| 38 | ITN 规则只覆盖少量例子 | 号码、数量、日期、金额应分类建模并保留 spoken form |
| 40 | 小型字符袋分类器 | 用于 NLU contract；不代表真实领域泛化 |
| 41 | 不调用外部 LLM | 重点是证据、schema、验证、确认和审计设计 |
| 42～45 | CPU 教学规模的前沿结构 | 用于验证 shape、目标函数和状态；不报告为工业模型质量 |
| 46 | 默认不下载或训练 Qwen3-ASR 权重 | 完成资源、许可、泄漏与基线检查后，才在真实硬件执行推理/微调 |
| 结课声学引擎 | 22 条合成数字训练语音 | 用于打通前端、CTC、解码、LM 和流式状态，不报告生产泛化率 |

## 后续真实项目的最低要求

- speaker-disjoint 的训练、验证和测试集；
- 固定文本规范和 CER/WER 脚本；
- 流式与离线分别评估；
- 前端噪声、设备、语速和长度分桶；
- FP32/INT8、不同 beam/LM/chunk 的质量—延迟曲线；
- 版本化模型、词表、LM、WFST graph、前端参数和语义 schema。
